# 블록 스키마

`data/*.js` 의 `blocks` 배열에 들어가는 콘텐츠 조각 정의입니다.
**하나의 데이터에서 마크다운과 웹 두 가지 출력이 나옵니다.**

원칙: **마크다운으로 표현할 수 없는 블록 타입은 만들지 않는다.**
채점 대상이 `.md` 이므로, md 에서 깨지는 표현은 웹에도 넣지 않습니다.

---

## 페이지 구조

```javascript
{
  id: 9,
  type: "multi",                 // single | multi
  navTitle: "new v1",            // 네비 라벨 + 마크다운 ## 헤딩
  subPages: [
    { title: "프롬프트 설계",     // 선택 — 마크다운 ### 헤딩
      blocks: [ /* 아래 7종 */ ] }
  ]
}
```

`title` 이 없으면 md 헤딩은 `### 9-1` 처럼 위치 라벨로 대체됩니다.

> **헤딩 중복 금지 (A안)**
> 서브페이지 `title` 이 이미 `###` 헤딩을 만든다. 따라서 그 페이지의 `blocks`
> 안에서 같은 제목을 `{ type:"text", tag:"h2" }` 로 **반복하지 않는다.**
> 반복하면 md 에서 `## 제목` 이 섹션 헤딩(`##`)과 레벨이 충돌해 목차가 어긋난다.
> 페이지 안의 소제목이 꼭 필요할 때만 `h3` 이하를 쓴다.

---

## 블록 타입 7종

### 1. `title` — 표지 전용 (섹션 1)

```javascript
{ type: "title", main: "3 Round Prompt Stress Test", sub: "for 3 LLMs" }
```

| 출력 | 결과 |
|------|------|
| md | `# 3 Round Prompt Stress Test` + 다음 줄에 `_for 3 LLMs_` |
| 웹 | h1 48px + 부제 24px, 세로 중앙 정렬 |

### 2. `text` — 텍스트

```javascript
{ type: "text", tag: "h2", value: "Evaluation Axis" }
{ type: "text", tag: "p",  value: "3가지 축으로 평가했다." }
```

`tag` 는 `h1` `h2` `h3` `p` 중 하나. `value` 안에는 마크다운 인라인 문법
(`**강조**`, `` `코드` ``, `- 목록`)을 쓸 수 있고 양쪽 출력에 모두 반영됩니다.

| 출력 | 결과 |
|------|------|
| md | `## 제목` / 본문 그대로 |
| 웹 | h2 32px / p 20px |

### 3. `image` — 이미지

```javascript
{ type: "image", src: "assets/images/s04-1-axis.png",
  caption: "MDI vs Permutation 중요도 비교", zoomable: true }
```

| 출력 | 결과 |
|------|------|
| md | `![캡션](assets/images/s04-1-axis.png)` + 캡션 줄 |
| 웹 | 이미지 + 캡션, `zoomable` 이면 클릭 시 라이트박스 |

- `zoomable` 은 **웹 전용**입니다. md 에서는 무시되고 이미지만 남습니다.
- `src` 는 `assets/images/...` 상대경로. `README.md` 와 `index.html` 이
  같은 폴더에 있으므로 양쪽에서 동일하게 해석됩니다.

### 4. `table` — 표

```javascript
{ type: "table",
  headers: ["항목", "v1", "v2", "v3"],
  rows: [
    ["형식 준수", "실패", "부분", { t: "성공", mark: "good" }],
    ["환각",     { t: "발생", mark: "bad" }, "없음", "없음"]
  ] }
```

셀은 **문자열** 또는 **`{ t, mark }`** 입니다. `mark` 는 `good` / `bad` 만 허용.

| 출력 | 결과 |
|------|------|
| md | 일반 마크다운 표 — `mark` 는 무시하고 `t` 값만 출력 |
| 웹 | 같은 표 + `mark` 셀에 옅은 배경색 (문단 스타일 방해하지 않는 수준) |

표는 **단순 격자만** 지원합니다. 셀 병합, 셀 안의 목록은 md 가 표현하지 못하므로 쓰지 않습니다.

### 5. `code` — 코드 블록 *(스펙에 없던 추가 타입)*

```javascript
{ type: "code", lang: "python", value: "import matplotlib.pyplot as plt\n..." }
```

| 출력 | 결과 |
|------|------|
| md | ` ```python ` 펜스로 감싼 코드 |
| 웹 | 고정폭 폰트 + 가로 스크롤 |

실제 대화에 matplotlib 코드가 4개 포함되어 있어 추가했습니다.

### 6. `label` — 강조 뱃지

```javascript
{ type: "label", value: "v3 FINAL", color: "blue" }
```

| 출력 | 결과 |
|------|------|
| md | `**［v3 FINAL］**` — 헤딩 바로 아래 줄 |
| 웹 | 페이지 상단 뱃지 |

`color` 는 `blue` / `green` / `red` / `gray`. **웹 전용**이며 md 는 색을 버립니다.

### 7. `chat` — 대화

```javascript
{ type: "chat",
  source: "assets/raw/gpt54-v2.txt",   // 전문 출처 (근거 자료)
  messages: [
    { role: "user", body: "핵심만 3가지로 추려주세요." },
    { role: "ai", model: "GPT-5.4", version: "v2",
      body: "Processor는 두 방식 모두 1위로 유지되어...\n\n| 순위 | MDI |\n|---|---|\n| 1 | Processor |" }
  ] }
```

`body` 는 **마크다운 문자열**입니다. 다른 블록과 달리 구조화하지 않습니다.

- 이유: 대화는 LLM 출력을 **그대로 옮긴 근거 자료**입니다. 채점 대상이므로
  원문을 재작성하면 안 되고, 원문이 이미 마크다운입니다.
- `body` 안에는 표, 목록, 코드 펜스가 들어올 수 있습니다.
  그래서 인용문(`>`)으로 감싸지 않습니다 — 중첩되면 표가 깨집니다.

| 출력 | 결과 |
|------|------|
| md | 화자 라벨(굵게) + 본문 전문 + `---` 구분선 |
| 웹 | 말풍선, 발췌만 표시 후 "전문 보기" 링크 (아래 규칙) |

md 출력 형태:

```markdown
**🧑 User**

핵심만 3가지로 추려주세요.

**🤖 GPT-5.4** · v2

Processor는 두 방식 모두 1위로 유지되어...

| 순위 | MDI |
|---|---|
| 1 | Processor |

---
```

---

## 웹 전용 필드 정리

md 에서 버려지는 값입니다. **없어도 내용이 통하도록** 작성합니다.

| 필드 | 블록 | md 에서 |
|------|------|---------|
| `zoomable` | image | 확대 기능 없음, 이미지는 남음 |
| `mark` | table 셀 | 색 없음, 값은 남음 |
| `color` | label | 색 없음, 텍스트는 남음 |
| `<!-- more -->` | chat body | 주석이라 렌더링되지 않음 — md 는 항상 전문 |

---

## 웹 발췌 규칙

md 는 항상 전문을 출력합니다. 웹만 발췌합니다.

| 우선순위 | 규칙 |
|---|---|
| 1 | `body` 에 `<!-- more -->` 가 있으면 그 앞까지 |
| 2 | 없으면 **700자** 한도 내에서 문단 경계까지 |
| 3 | 원문이 700자 미만이면 전문 그대로 |

```javascript
body: "Processor는 두 방식 모두 1위로 유지되어...\n\n" +
      "<!-- more -->\n\n" +
      "RAM은 MDI 3위에서 Permutation 2위로 상승..."
```

- `<!-- more -->` 는 HTML 주석이므로 마크다운에서 **보이지 않습니다.** 원본은 하나입니다.
- 문단 수(`webParas`)가 아니라 글자 수를 쓰는 이유: 실측 결과 문단 길이가
  3자 ~ 1,017자로 편차가 커서(중앙값 120자) 문단 수는 분량 지표가 되지 못합니다.
- `700` 은 20px 폰트 · 100vh 기준값입니다.

---

## 긴 대화를 나누는 방법

**자동 페이지네이션은 쓰지 않습니다.** 넘치는 분량을 렌더러가 알아서 다음 페이지로
넘기면 서브페이지 개수가 화면 크기·폰트·브라우저에 따라 달라지고,
`#9-3` 이 기기마다 다른 내용을 가리키게 됩니다.

대신 **턴 묶음을 데이터에 명시**합니다. 서브페이지 개수는 언제나 `subPages.length` 입니다.

```javascript
{ id: 9, type: "multi", navTitle: "new v1", subPages: [
    { title: "초기 프롬프트", blocks: [ { type:"chat", messages:[ /* 1~2턴 */ ] } ] },
    { title: "분량 압축 요구", blocks: [ { type:"chat", messages:[ /* 3~4턴 */ ] } ] },
    { title: "시각화 요구",   blocks: [ { type:"chat", messages:[ /* 5~6턴 */ ] } ] },
    { title: "최종 형식",     blocks: [ { type:"chat", messages:[ /* 7~10턴 */ ] } ] }
] }
```

- 한 서브페이지 안에서도 개별 답변이 길면 위의 발췌 규칙이 적용됩니다.
- md 는 `### 초기 프롬프트` 처럼 서브페이지 단위로 헤딩이 생기고, 그 아래 전문이 들어갑니다.
- 웹과 md 의 구조가 1:1 로 유지됩니다.

---

## 원문 정제 규칙

복사한 `.txt` 에는 웹 UI 잔재가 섞여 있어 `data/*.js` 에 넣기 전 제거합니다.

| 잔재 | 처리 |
|------|------|
| `📋 복사` | 삭제 |
| 코드 앞 단독 `python` 줄 | `code` 블록의 `lang` 으로 이동 |
| 탭(`\t`) 구분 표 | 마크다운 파이프 표로 복원 |

원본 `.txt` 는 `assets/raw/` 에 손대지 않고 보관합니다.
